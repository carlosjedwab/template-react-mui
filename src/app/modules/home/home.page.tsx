import { PageWrapper } from 'app/components/obj.page-wrapper/page-wrapper.component';
import { Typography } from '@mui/material';

const HomePage = () => {
  return (
    <PageWrapper>
      <Typography variant="h3">Home Page</Typography>
    </PageWrapper>
  );
};

export default HomePage;
